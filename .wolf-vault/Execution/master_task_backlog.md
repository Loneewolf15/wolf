# 🧭 Wolf Master Task Backlog — Phase Ordered
> Generated by Compass · 2026-04-10 · Open bugs: **None**

---

## ✅ Completed Infrastructure (Pre-Phase)
- [x] LLVM IR backend (native code emission)
- [x] `wolf.config` INI system (compile-time constants)
- [x] Per-request memory arena (`wolf_req_alloc`)
- [x] MySQL connection pool + graceful shutdown
- [x] Redis (real hiredis), Postgres
- [x] HTTP server (graceful shutdown, timeout, SIGPIPE)
- [x] Multipart file uploads (`wolf_http_req_file`, `wolf_file_save`)
- [x] JWT, OpenSSL, libsodium crypto
- [x] WebSocket (RFC 6455, Phoenix Channels)
- [x] `@supervise` + Let-It-Crash thread spawner
- [x] Lock-free telemetry hash map (`/dash` endpoint)
- [x] OOM guard in `wolf_req_alloc` + `wolf_req_strdup`
- [x] `wolf_is_dedicated_server()` + CPU affinity + SCHED_RR
- [x] `WOLF_FREESTANDING` bare-metal split
- [x] Benchmarks (Wolf vs Go vs Node vs Python — Wolf wins)

---

## 🔄 Phase 2 — Stdlib Completion  ← CURRENT

> Compass Rule: **All stdlib must be complete before Phase 3 DB layer expands.**

- [x] STDLIB-01: String functions + regex
- [x] STDLIB-02: Array functions (fill, chunk, column, product)
- [x] STDLIB-05: Security (Argon2id, JWT, XSalsa20, RSA, Curve25519)
- [x] STDLIB-08: Validation rules engine
- [x] **STDLIB-03**: Math — `array_mean`, `array_std_dev` ✅ (already fully wired)
- [x] **STDLIB-04**: Date/time — object methods, timezone constants ✅ (already fully wired)
- [x] **STDLIB-06**: HTTP client — outbound GET/POST/PUT/DELETE/PATCH + set_header ✅
- [x] **STDLIB-07**: File system — `copy`, `move`, `scan_dir`, `path_join`, `dir_create` ✅
- [x] **STDLIB-09**: Type casting — `typeof`, `is_string/int/float/bool/null/array`, `settype` ✅
- [x] **STDLIB-10**: Wolf-specific — `money_*`, `pluralise`, `phone_format`, `log_debug`, `retry_sleep` ✅
- [ ] **Unblocked now**: `slug()` + `truncate()` — verify wired in emitter
- [ ] **Real MSSQL** — install `freetds-dev`, replace mock *(blocked on system dep)*

---

## ⬜ Phase 3 — Database Layer

> Compass Rule: **Phase 2 stdlib must be ≥80% done before expanding DB layer.**

- [x] DB-01: Query builder (`where`, `orderBy`, `get`)
- [x] **DB-02**: Eager loading — `$db->with("rel", "fk", "table", "pk")` ✅
- [x] **DB-03**: N+1 detection — compiler warning on queries inside loops (non-fatal, IsWarning) ✅
- [x] **DB-04**: Pagination — `$db->paginate(page, per_page)` ✅ (already fully wired, inherits eager loading)

---

## ⬜ Phase 4 — Language Completeness

> Compass Rule: **DB layer (Phase 3) must be done before language completeness unlocks.**

Order within phase (dependency-first):
- [x] **Enums** — native enum types (simplest, no deps) ✅
- [x] **Error handling** — `try/catch` propagation (depends on: parser)
- [ ] **Closures / first-class functions** *(partial: lexer has closures)*
- [ ] **Pattern matching** — `match` with exhaustiveness check
- [ ] **Interfaces / traits**
- [ ] **Generics** — type parameters (depends on: interfaces)
- [ ] **@safe blocks** — error-safe execution
- [ ] **@guard blocks** — authentication as language construct
- [ ] **@queue + @cache blocks** — background jobs + caching
- [ ] **@contract blocks** — API contract testing in CI
- [ ] **Built-in pub/sub** — without external broker
- [ ] **Package system** — `wolf.mod` + `wolfpkg`
- [ ] **Goroutines + Channels** — `wolf_async`, `wolf_await`, `channel(type)`

---

## ⬜ Phase 5 — Real-time & Protocol

> **Note:** WebSocket runtime is ✅ done. What remains is the *language-layer* API.

- [ ] **WebSocket language API** — `$this->res->websocket()`, `broadcast()`, `presence_track()` in Wolf syntax
- [ ] **GraphQL auto-generation** — `@graphql` annotations on models
- [ ] **gRPC support** — `@grpc` service definitions

---

## ⬜ Phase 6 — Tooling

> Compass Rule: **Language completeness (Phase 4) must be green before heavy tooling.**

- [x] `wolf new` — Smart Mode (script + API + Docker templates)
- [ ] **TOOL-01**: Auto-discovery — `wolf.config` dirs array (controllers, models, services)
- [ ] **wolf migrate** — database migration management *(high DX value, can ship earlier)*
- [ ] **wolf fmt** — code formatter
- [ ] **wolf dev** — entire stack in one command, zero config
- [ ] **wolf generate** — add features to existing project
- [ ] **wolf test** — built-in test runner with mocking
- [ ] **TOOL-02**: Dynamic test generation from `@contract` definitions
- [ ] **wolf docker init** — generate `docker-compose.yml` from `wolf.config`
- [ ] **wolf profile** — production profiler without APM
- [ ] **wolf explain** — AI-powered error explanation
- [ ] **wolf deploy --hot** — zero-downtime hot code reload

---

## ⬜ Phase 7 — Observability & Security Defaults

- [ ] **OBS-01**: `metrics_increment/gauge/histogram` built-in functions *(telemetry C layer exists — needs Wolf syntax)*
- [ ] **OBS-02**: Automatic `GET /health` on every Wolf app
- [ ] **OBS-03**: `wolf dev` dashboard at `localhost:8081`
- [ ] **OBS-04**: OpenTelemetry export
- [ ] **SEC-01**: Automatic security headers on every response
- [ ] **SEC-02**: SQL injection compiler warning (string interpolation in `query()`)
- [ ] **SEC-03**: Secret redaction in logs
- [ ] **SEC-04**: `$this->req->audit()` — request audit trail
- [ ] **SEC-05**: `$this->req->idempotent()` — idempotency tracking
- [ ] **SEC-06**: `$this->req->diff()` — object diff

---

## ⬜ Phase 8 — Package Manager

- [ ] **PKG-01**: `wolf.mod` module file *(no downloading — just identifies module)*
- [ ] **PKG-02**: `wolfpkg` CLI — install, publish, search *(3–6 months post-launch)*
- [ ] **PKG-03**: `wolfpkg.dev` registry *(6–12 months post-launch)*

---

## ⬜ Phase 9 — Self-Hosting (Year 1)

- [ ] Re-implement lexer in Wolf
- [ ] Re-implement parser in Wolf
- [ ] Re-implement emitter in Wolf
- [ ] Wolf compiler compiles itself (bootstrap)
- [ ] Go compiler kept as parity/fallback

---

## ⬜ Phase 10 — Go Independence (Year 1.5)

- [ ] Go compiler retired
- [ ] All stdlib written in Wolf + C runtime only

---

## ⬜ Phase 11 — Ecosystem

- [ ] Wolf LSP (language server protocol)
- [ ] VS Code extension (syntax + LSP)
- [ ] Public benchmarks vs Rust/Go/C
- [ ] `wolfpkg.dev` fully live
- [ ] WASM target (Wolf in the browser)

---

## ⬜ Phase 12 — Embedded & Distribution

- [ ] **DIST-01**: `brew` / `winget` / `install.sh` distribution
- [ ] **EMB-01**: Zero-flag builds via `wolf.config` (`device = "esp32"`)
- [ ] **EMB-02**: Pure-C emission for ultra-constrained targets (Arduino Uno)

---

## 🧭 Compass Verdict — Next Sprint

| Task | Score | Verdict |
|---|---|---|
| STDLIB-06: HTTP client (outbound requests) | 5×F + 3×S = **8/10** | ✅ **GO — P1** |
| STDLIB-03/04: Math stats + Date object methods | 4×S + 2×D = **6/10** | ✅ **GO — P2** |
| STDLIB-07: File system (`copy`, `move`, `scan_dir`) | 3×S + 2×D = **5/10** | ✅ **GO — P3** |
| DB-02: Eager loading | 5×F = **5/10** | ⚠️ **DEFER** — unblock after stdlib ≥80% |
| Phase 4 (Enums, Closures) | 2×D + 1×X = **3/10** | ⚠️ **DEFER** — unblock after Phase 3 |
