func handle_redirect() {
    $url = $r->URL->Query()->Get("url")

    # Native Go method calls via Wolf!
    $w->Header()->Set("Location", $url)
    $w->WriteHeader(302)
}

route("GET", "/go", handle_redirect)
serve(8081)
