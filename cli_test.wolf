$count = argc()
print("Arg count: {$count}")

$i = 0
while $i < $count {
    $val = argv($i)
    print("Arg {$i}: {$val}")
    $i = $i + 1
}
# Using the standard // comments now supported!
// This should also work.
