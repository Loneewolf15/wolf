class UserModel {
    public $db = nil
    func findUserById($id) {
        return $this->db
    }
}
func myHandler($req, $res) {
    var $str = "hello, this is a very long string to ensure we allocate memory inside the arena! Let's allocate some more: "
    var $arr = [$str, $str, $str]
    var $dict = {"a": $arr}

    var $u = nil
    var $res2 = $u->findUserById(1)
    
    $res->status(200)
    $res->json($dict)
}

wolf_http_serve(8080, "myHandler")
